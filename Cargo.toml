[package]
name = "cst92xx"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-hal = "1.0.0"
embedded-hal-async = "1.0.0"
heapless = "0.8"
embassy-time = "0.5.1"
defmt = { version = "1.1.1", optional = true }

[features]
defmt = ["dep:defmt"]
